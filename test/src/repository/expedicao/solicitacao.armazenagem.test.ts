import { v4 as uuidv4 } from 'uuid';
import { io, Socket } from 'socket.io-client';
import { expect, test, describe, beforeAll } from 'vitest';

describe('CRUD - SolicitacaoArmazenagem', () => {
  beforeAll(() => async () => {});

  test('', async () => {});
});
